// Re-exportar o novo componente para manter compatibilidade
import CustomDomainManager from "./CustomDomainManager";

// Manter o nome antigo para compatibilidade com imports existentes
const CustomDomainDialog = CustomDomainManager;

export default CustomDomainDialog;
